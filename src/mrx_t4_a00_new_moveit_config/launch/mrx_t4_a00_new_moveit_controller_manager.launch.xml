<launch>
    <!-- 设置 trajectory_execution_manager 查找控制器插件所需的参数 -->
    <arg name="moveit_controller_manager" default="moveit_simple_controller_manager/MoveItSimpleControllerManager" /> 
    <param name="moveit_controller_manager" value="$(arg moveit_controller_manager)" />

    <!-- 加载controllers -->
    <!-- 加载Arbotix -->  
    <!-- <rosparam file="$(find mrx_t4_a00_new_moveit_config)/config/controllers.yaml" /> -->

    <!-- 加载Gazebo -->
    <rosparam file="$(find mrx_t4_a00_new_moveit_config)/config/controllers_gazebo.yaml" />
</launch>
